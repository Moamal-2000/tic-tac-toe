import s from "./MainMenu.module.scss";

const MainMenu = () => {
  return (
    <div className={s.menuOverlay}>
      <section className={s.mainMenu}></section>
    </div>
  );
};

export default MainMenu;
